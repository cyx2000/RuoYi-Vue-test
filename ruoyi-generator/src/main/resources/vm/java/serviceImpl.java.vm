package ${packageName}.service.impl;

import java.util.List;

import jakarta.annotation.Resource;
import org.springframework.stereotype.Service;
#if($table.sub)
import org.springframework.transaction.annotation.Transactional;
import ${packageName}.domain.${subClassName};
#end
import com.ruoyi.common.core.page.TableDataInfo;
import ${packageName}.repository.${ClassName}Repository;
#if($table.sub)
import ${packageName}.repository.${subClassName}Repository;
#end
import ${packageName}.domain.${ClassName};
import ${packageName}.service.I${ClassName}Service;

/**
 * ${functionName}Service业务层处理
 *
 * @author ${author}
 * @date ${datetime}
 */
@Service
public class ${ClassName}ServiceImpl implements I${ClassName}Service
{
    @Resource
    private ${ClassName}Repository ${className}Repository;

#if($table.sub)
    @Resource
    private ${subClassName}Repository ${subclassName}Repository;
#end

    /**
     * 查询${functionName}
     *
     * @param ${pkColumn.javaField} ${functionName}主键
     * @return ${functionName}
     */
    @Override
    public ${ClassName} select${ClassName}By${pkColumn.capJavaField}(${pkColumn.javaType} ${pkColumn.javaField})
    {
        return ${className}Repository.select${ClassName}By${pkColumn.capJavaField}(${pkColumn.javaField});
    }

#if($table.crud || $table.sub)
    /**
     * 根据条件分页查询${functionName}列表
     *
     * @param ${className} ${functionName}
     * @return 分页完成的${functionName}集合
     */
    @Override
    public TableDataInfo getPagedListResp(${ClassName} ${className})
    {
        return ${className}Repository.getPagedListResp(${className});
    }

#end
    /**
     * 查询${functionName}列表
     *
     * @param ${className} ${functionName}
     * @return ${functionName}
     */
    @Override
    public List<${ClassName}> select${ClassName}List(${ClassName} ${className})
    {
        return ${className}Repository.select${ClassName}List(${className});
    }

    /**
     * 新增${functionName}
     *
     * @param ${className} ${functionName}
     * @return 结果
     */
#if($table.sub)
    @Transactional
#end
    @Override
    public int insert${ClassName}(${ClassName} ${className})
    {
#if($table.sub)
        int rows = ${className}Repository.insert${ClassName}(${className});
        ${subClassName} ${subclassName} = new ${subClassName}();
        // TODO 设置对应的数据
        ${subclassName}Repository.insert${subClassName}(${subclassName});
        return rows;
#else
        return ${className}Repository.insert${ClassName}(${className});
#end
    }

    /**
     * 修改${functionName}
     *
     * @param ${className} ${functionName}
     * @return 结果
     */
#if($table.sub)
    @Transactional
#end
    @Override
    public int update${ClassName}(${ClassName} ${className})
    {
#if($table.sub)
        ${className}Repository.delete${subClassName}By${subTableFkClassName}(${className}.get${pkColumn.capJavaField}());
        ${subClassName} ${subclassName} = new ${subClassName}();
        // TODO 设置对应的数据
        ${subclassName}Repository.insert${subClassName}(${subclassName});
#end
        return ${className}Repository.update${ClassName}(${className});
    }

    /**
     * 批量删除${functionName}
     *
     * @param ${pkColumn.javaField}s 需要删除的${functionName}主键
     * @return 结果
     */
#if($table.sub)
    @Transactional
#end
    @Override
    public int delete${ClassName}By${pkColumn.capJavaField}s(${pkColumn.javaType}[] ${pkColumn.javaField}s)
    {
#if($table.sub)
        ${className}Repository.delete${subClassName}By${subTableFkClassName}s(${pkColumn.javaField}s);
#end
        return ${className}Repository.delete${ClassName}By${pkColumn.capJavaField}s(${pkColumn.javaField}s);
    }

    /**
     * 删除${functionName}信息
     *
     * @param ${pkColumn.javaField} ${functionName}主键
     * @return 结果
     */
#if($table.sub)
    @Transactional
#end
    @Override
    public int delete${ClassName}By${pkColumn.capJavaField}(${pkColumn.javaType} ${pkColumn.javaField})
    {
#if($table.sub)
        ${className}Repository.delete${subClassName}By${subTableFkClassName}(${pkColumn.javaField});
#end
        return ${className}Repository.delete${ClassName}By${pkColumn.capJavaField}(${pkColumn.javaField});
    }
}
